<?xml version="1.0" encoding="UTF-8"?>
<project
	xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>de.eldecker.dhbw.spring</groupId>
	<artifactId>antwortengenerator</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<name>Antwortengenerator mit KI</name>
	<description>Antworten für Single-Choice-Frage mit KI generieren</description>
	<inceptionYear>2026</inceptionYear>


	<properties>

		<java.version>21</java.version>
		<spring-ai.version>2.0.0-RC2</spring-ai.version>

		<!--
			Unit-Tests überspringen, weil es keine gibt und sonst der Maven-Prozess
			blockiert, weil er auf Eingabe von STDIN wartet.
		 -->
		<maven.test.skip>true</maven.test.skip>

	</properties>

	<dependencies>

		<!-- Abhängigkeit in start.spring.io: "Google GenAI" -->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-google-genai</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>tools.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>3.2.0</version><!-- neues Version: https://mvnrepository.com/artifact/tools.jackson.core/jackson-databind -->
		</dependency>

	</dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
